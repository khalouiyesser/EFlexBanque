
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="{{ asset('login/style.css') }}" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <title>Sign in & Sign up Form</title>
</head>
<body>
<div class="container">
  <div class="forms-container">
    <div class="signin-signup">
        <form action="{{ path('app_loginClient') }}" method="post" class="sign-in-form"> 
          {% if app.user %}
            <div class="mb-3">
              You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
            </div>
          {% endif %}
          <h2 class="title">Se Connecter</h2>
          <div class="input-field">
            <i class="fas fa-user"></i>
            <input type="text" class="input" name="email" id="inputEmail"  autocomplete="email" required autofocus placeholder="Identifiant">
          </div>
          <div class="input-field">
            <i class="fas fa-lock"></i>
            <input type="password" class="input" name="password" id="inputPassword" autocomplete="current-password" required placeholder="Mot de passe" />
          </div>
          <div>
           <a href="{{ path ('mdpoublie') }}">Forgot Password?</a>
          </div>
          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
          <input type="submit" class="btn" value="Se connecter">
        </form>
           <form action="#" class="sign-up-form">
            <h2 class="title">Créer un compte</h2>
            <div class="input-field">
              <i class="bi bi-cash-coin"></i>
              <input type="text" placeholder="Identifiant" />
            </div>


            <div class="input-field">
              <i class="fas fa-user"></i>
              <input type="text" placeholder="CIN" />
            </div>
            <div class="input-field">
              <i class="bi bi-telephone-fill"></i>
              <input type="text" placeholder="Numéro de téléphone" />
            </div>
            <div class="input-field">
              <i class="bi bi-calendar"></i>
              <input type="text" placeholder="date de naissance" />
            </div>

            <div class="input-field">
              <i class="bi bi-house-door"></i>
              <input type="text" placeholder="adresse" />
            </div>
            
            <div class="input-field">
              <i class="bi bi-person-workspace"></i>
              <input type="text" placeholder="profesion" />
            </div>

            <div class="input-field">
              <i class="fas fa-envelope"></i>
              <input type="email" placeholder="Email" />
            </div>
            <div class="input-field">
              <i class="fas fa-lock"></i>
              <input type="password" placeholder="Mot de passe" />
            </div>
            <div>
              <label for="conditions">J'accepte les termes et conditions du service bancaire en ligne ainsi que la politique de confidentialité </label>
              <input type="checkbox" id="conditions" name="conditions" required> 
            </div>
            <input type="submit" class="btn" value="S'inscrire" />
            </form>
        </div>
      </div>

      <div class="panels-container">
        <div class="panel left-panel">
          <div class="content">
            <h3>Vous n'avez pas de comptee?</h3>
            <p>
              Innover , Economiser , Prospérer : Tout en ligne !
            </p>
            <button class="btn transparent" id="sign-up-btn" onclick="window.location.href='{{ path('app_register') }}'">
                    S'inscrire
                   </button>
          </div>
          <img src="login/img/log.svg" class="image" alt="" />
        </div>
        <div class="panel right-panel">
          <div class="content">
    
            <h3>Vous avez déjà un compte ?</h3>
            <p>
              Innover , Economiser , Prospérer : Tout en ligne !
            </p>
            <button class="btn transparent" id="sign-in-btn">
              Se connecter
            </button>
          </div>
          <img src="login/img/register.svg" class="image" alt="" />
        </div>
      </div>
    </div>

    <script src="login/app.js"></script>
  </body>
</html>

