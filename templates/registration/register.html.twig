

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="{{ asset('login/style.css') }}" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <title>Sign in & Sign up Form</title>
</head>
<body>
<div class="container">
  <div class="forms-container">
    <div class="signin-signup">  
        <form action="{{ path('app_loginClient') }}" method="post" class="sign-in-form">
            <h2 class="title">Se Connecter</h2>
          {% if app.user %}
            <div class="mb-3">
              You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
            </div>
          {% endif %}
          <div class="input-field">
            <i class="fas fa-user"></i>
            <input type="text" class="input" name="email" id="inputEmail" value="" autocomplete="email" required autofocus placeholder="Identifiant">
          </div>
          <div class="input-field">
            <i class="fas fa-lock"></i>
            <input type="password" class="input" name="password" id="inputPassword" autocomplete="current-password" required placeholder="Mot de passe" />
          </div>
          <div>
            <input type="checkbox" class="form-check-input" id="basic_checkbox_1">
            <label class="form-check-label" for="basic_checkbox_1">Mot de passe oublié ?</label>
          </div>
          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
          <input type="submit" class="btn" value="Se connecter">
        </form>

        <form action="{{ path('app_register') }}" method="post" class="sign-up-form">
         {{ form_errors(registrationForm) }}
         {{ form_start(registrationForm) }}
       
          <h2 class="title">Créer un compte</h2>
          <div class="input-field">
            <i class="bi bi-cash-coin"></i>
            {{ form_row(registrationForm.email, {'attr': {'class': 'form-control'}, 'id': 'email'}) }}
          </div>
          <div class="input-field">
            <i class="fas fa-user"></i>
            {{ form_row(registrationForm.name, {'attr': {'class': 'form-control'}, 'id': 'name'}) }}
            
          </div>
          <div class="input-field">
            <i class="bi bi-telephone-fill"></i>
            {{ form_row(registrationForm.tel, {'attr': {'class': 'form-control'}, 'id': 'tel'}) }}
          </div>
          <div class="input-field">
            <i class="fas fa-envelope"></i>
             {{ form_row(registrationForm.adresse, {'attr': {'class': 'form-control'}, 'id': 'adresse'}) }}
            
          </div>
          <div class="input-field">
            <i class="fas fa-lock"></i>
             {{ form_row(registrationForm.plainPassword, {
            label: 'Password'  }) }}
            
          </div>
          <div>
            {{ form_row(registrationForm.agreeTerms) }}
          </div>

{#          <div class="col-md-4 offset-md-4 text-center" >#}
{#            <div class="form-group">#}
{#              {{ form_row(registrationForm.recaptcha) }}#}
{#              {{ form_errors(registrationForm.recaptcha,{'attr':{'class': 'text-danger'}}) }}#}
{#            </div>#}
{#          </div>#}

        <button type="submit" class="btn">Register</button>
   
         {{ form_end(registrationForm) }}
        </form>
      </div>
    </div>
 
  <div class="panels-container">
    <div class="panel left-panel">
      <div class="content">
        <h3>Vous n'avez pas de compte?</h3>
        <p>Innover, Economiser, Prospérer : Tout en ligne !</p>
        <button class="btn transparent" id="sign-up-btn">S'inscrire</button>
      </div>
      <img src="login/img/log.svg" class="image" alt="" />
    </div>
    <div class="panel right-panel">
      <div class="content">
        <h3>Vous avez déjà un compte ?</h3>
        <p>Innover, Economiser, Prospérer : Tout en ligne !</p>
        <button class="btn transparent" id="sign-in-btn">Se connecter</button>
      </div>
      <img src="login/img/register.svg" class="image" alt="" />
    </div>
  </div>
</div>
<script src="login/app.js"></script>
</body>
</html>


